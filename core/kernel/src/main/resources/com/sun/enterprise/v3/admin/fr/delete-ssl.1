DELETE-SSL(1)                                                    DELETE-SSL(1)

NOM
       delete-ssl - supprime l'élément SSL dans le processus d'écoute HTTP, le
       processus d'écoute IIOP ou le service IIOP sélectionné

SYNOPSIS
           delete-ssl [--help]
           [--target target]
           --type listener_or_service_type
           listener_id

DESCRIPTION
       La sous-commande delete-ssl supprime l'élément SSL dans le processus
       d'écoute HTTP, le processus d'écoute IIOP ou le service IIOP
       sélectionné.

       L'élément listener_id n'est pas requis si le type est iiop-service.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       Si une option possède un nom d'option court, l'option courte précède le
       nom d'option long. Les options courtes contiennent un tiret alors que
       les options longues en contiennent deux.

       --help, -?
           Affiche le texte d'aide de la sous-commande.

       --target
           Spécifie la cible sur laquelle vous configurez l'élément SSL. Les
           valeurs suivantes sont valides :

           server
               Spécifie le serveur sur lequel le service IIOP ou le processus
               d'écoute HTTP/IIOP doit être déconfiguré pour SSL.

           config
               Spécifie la configuration contenant le processus
               d'écoute HTTP/IIOP ou le service IIOP pour lequel SSL doit être
               déconfiguré.

           cluster
               Spécifie le cluster dans lequel le processus d'écoute HTTP/IIOP
               ou le service IIOP doit être déconfiguré pour SSL. SSL sera
               déconfiguré sur toutes les instances de serveur du cluster pour
               le processus d'écoute ou le service IIOP respectif.

           instance
               Spécifie l'instance dans laquelle le processus
               d'écoute HTTP/IIOP ou le service IIOP doit être déconfiguré
               pour SSL.

       --type
           Type de service ou de processus d'écoute pour lequel SSL est
           supprimé. Il doit s'agir de l'un des types suivants :

           ·   http-listener

           ·   iiop-listener

           ·   iiop-service

OPÉRANDES
       listener_id
           ID du processus d'écoute duquel l'élément SSL doit être supprimé.

           L'opérande listener_id n'est pas requis si l'option --type a la
           valeur iiop-service.

EXEMPLES
       Exemple 1, Suppression d'un élément SSL d'un processus d'écoute HTTP
           L'exemple suivant montre comment supprimer un élément SSL d'un
           processus d'écoute HTTP nommé http-listener-1.

               asadmin> delete-ssl
               --type http-listener http-listener-1
               Command delete-ssl executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-ssl(1)

       asadmin(1M)

                                 22 déc. 2010                    DELETE-SSL(1)
