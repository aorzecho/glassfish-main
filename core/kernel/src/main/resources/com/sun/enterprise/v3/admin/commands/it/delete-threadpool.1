DELETE-THREADPOOL(1)                                      DELETE-THREADPOOL(1)

NOME
       delete-threadpool - rimuove un pool di thread

SINTASSI
           delete-threadpool [--help] [--target target] threadpool-id

DESCRIZIONE
       Rimuove il pool del thread con l'ID specificato. Questo comando
       secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Questa opzione specifica la destinazione da cui si rimuove il pool
           del thread.

           I valori validi sono i seguenti:

           server
               Elimina il pool di thread per l'istanza di GlassFish Server
               predefinita server ed è il valore predefinito

           configuration-name
               Elimina il pool del thread per la configurazione specificata.

           cluster-name
               Elimina il pool del thread per tutte le istanze del cluster.

           instance-name
               Elimina il pool del thread per una particolare istanza.

OPERANDI
       threadpool-id
           ID della coda di lavoro, ad esempio thread-pool1,
            threadpool-2 e così via.

ESEMPI
       Esempio 1, Eliminazione di un pool del thread
           In questo esempio viene eliminato threadpool-l.

               asadmin> delete-threadpool threadpool-1
               Command delete-threadpool executed successfully

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-threadpool(1), list-threadpools(1)

       asadmin(1M)

                               29 novembre 2010           DELETE-THREADPOOL(1)
