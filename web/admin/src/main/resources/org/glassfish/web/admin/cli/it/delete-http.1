DELETE-HTTP(1)                                                  DELETE-HTTP(1)

NOME
       delete-http - rimuove i parametri HTTP da un protocollo

SINTASSI
           delete-http [--help]
           [--target target]
           protocol-name

DESCRIZIONE
       Il comando secondario delete-http rimuove da un protocollo l'insieme di
       parametri HTTP specificato. Questo comando secondario è supportato solo
       in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Elimina il parametro HTTP impostato solo dalla destinazione
           specificata. I valori validi sono i seguenti:

           server
               Elimina il set di parametri HTTP dall'istanza server
               predefinita. Questo è il valore predefinito.

           configuration-name
               Elimina il parametro HTTP impostato dalla configurazione
               specificata.

           cluster-name
               Elimina il parametro HTTP impostato da tutte le istanze server
               nel cluster specificato.

           standalone-instance-name
               Elimina il parametro HTTP impostato dall'istanza server
               standalone specificata.

OPERANDI
       protocol-name
           Nome del protocollo da cui eliminare l'insieme di parametri HTTP.

ESEMPI
       Esempio 1, Eliminazione di un insieme di parametri HTTP
           Il seguente comando elimina l'insieme di parametri HTTP da un
           protocollo denominato http-1:

               asadmin> delete-http http-1
               Command delete-http executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-http(1)

       asadmin(1M)

                                08 ottobre 2010                 DELETE-HTTP(1)
