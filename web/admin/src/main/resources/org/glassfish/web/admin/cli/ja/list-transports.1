LIST-TRANSPORTS(1)                                          LIST-TRANSPORTS(1)

名前
       list-transports - 既存のトランスポートを一覧表示します

概要
           list-transports [--help]
           [target]

説明
       list-transportsサブコマンドは、既存のトランスポートを一覧表示します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           一覧表示する内容を、指定したターゲットのトランスポートに制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのトランスポートを一覧表示します。この値がデフォルト値です。

           configuration-name
               指定した構成のトランスポートを一覧表示します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスのトランスポートを一覧表示します。

           instance-name
               指定したサーバー・インスタンスのトランスポートを一覧表示します。

例
       例 1, トランスポートの一覧表示
           次のコマンドは、サーバー・インスタンスのすべてのトランスポートを一覧表示します。

               asadmin> list-transports
               http1-trans
               tcp
               Command list-transports executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-transport(1), delete-transport(1)

       asadmin(1M)

                                 2010年10月8日              LIST-TRANSPORTS(1)
