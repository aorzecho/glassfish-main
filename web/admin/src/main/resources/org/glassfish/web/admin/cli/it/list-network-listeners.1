LIST-NETWORK-LISTE(1)                                    LIST-NETWORK-LISTE(1)

NOME
       list-network-listeners - elenca i listener di rete esistenti

SINTASSI
           list-network-listeners [--help]
           [target]

DESCRIZIONE
       Il comando secondario list-network-listeners elenca i listener di rete
       esistenti. Questo comando secondario è supportato solo in modalità
       remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Limita l'elenco ai listener di rete per una destinazione
           specificata. I valori validi sono i seguenti:

           server
               Elenca i listener di rete per l'istanza server predefinita.
               Questo è il valore predefinito.

           configuration-name
               Elenca i listener di rete per la configurazione specificata.

           cluster-name
               Elenca i listener di rete per tutte le istanze server del
               cluster specificato.

           instance-name
               Elenca i listener di rete per l'istanza server specificata.

ESEMPI
       Esempio 1, Visualizzazione dell'elenco dei listener di rete
           Il seguente comando elenca tutti i listener di rete per l'istanza
           server:

               asadmin> list-network-listeners
               admin-listener
               http-listener-1
               https-listener-2
               Command list-network-listeners executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-network-listener(1), delete-network-listener(1)

       asadmin(1M)

                                8 ottobre 2010           LIST-NETWORK-LISTE(1)
