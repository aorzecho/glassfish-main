CREATE-HTTP-REDIRE(1)                                    CREATE-HTTP-REDIRE(1)

NOM
       create-http-redirect - ajoute un nouveau réacheminement HTTP

SYNOPSIS
           create-http-redirect [--help]
           [--redirect-port redirect-port]
           [--secure-redirect={false|true}]
           [--target target]
           protocol-name

DESCRIPTION
       La sous-commande create-http-redirect crée un réacheminement HTTP.
       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Affiche le texte d'aide de la sous-commande.

       --redirect-port
           Numéro de port pour les réacheminements. Si le processus
           d'écoute HTTP prend en charge des demandes non SSL, et une demande
           pour laquelle une contrainte de sécurité correspondante requiert le
           transport SSL est reçue, GlassFish Server réachemine
           automatiquement la demande vers ce numéro de port.

       --secure-redirect
           Si la valeur est true, le réacheminement HTTP exécute un
           protocole SSL. La valeur par défaut est false.

       --target
           Crée le réacheminement HTTP uniquement sur la cible indiquée. Les
           valeurs valides sont les suivantes :

           server
               Crée le réacheminement HTTP sur l'instance de serveur par
               défaut. Ceci est la valeur par défaut.

           configuration-name
               Crée le réacheminement HTTP dans la configuration indiquée.

           cluster-name
               Crée le réacheminement HTTP sur toutes les instances de serveur
               dans le cluster indiqué.

           standalone-instance-name
               Crée le réacheminement HTTP sur l'instance de serveur autonome
               indiquée.

OPÉRANDES
       protocol-name
           Nom du protocole auquel appliquer le réacheminement.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       delete-http-redirect(1)

       asadmin(1M)

                                 08 oct. 2010            CREATE-HTTP-REDIRE(1)
