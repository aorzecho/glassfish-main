LIST-HTTP-LISTENER(1)                                    LIST-HTTP-LISTENER(1)

參考名
       list-http-listeners - 列出現有的網路偵聽程式

概要
           list-http-listeners [--help]
           [target]

說明
       list-http-listeners 子命令會列出現有的網路偵聽程式。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

運算元
       目
           將清單限制為指定目標的網路偵聽程式。有效的值如下：

           server
               列出預設伺服器實例的網路偵聽程式。此為預設值。

           configuration-name
               列出指定組態的網路偵聽程式。

           cluster-name
               列出指定叢集內所有伺服器實例的網路偵聽程式。

           instance-name
               列出指定伺服器實例的網路偵聽程式。

範例
       範例 1, 列出網路偵聽程式
           下列命令會列出伺服器實例的所有網路偵聽程式：

               asadmin> list-http-listeners
               http-listener-1
               http-listener-2
               admin-listener
               Command list-http-listeners executed successfully.

結束狀態
       0
           命令執行成功

       1
           執行命令時發生錯誤

另請參閱
       create-http-listener(1), delete-http-listener(1)

       asadmin(1M)

                              2011 年 2 月 11 日         LIST-HTTP-LISTENER(1)
