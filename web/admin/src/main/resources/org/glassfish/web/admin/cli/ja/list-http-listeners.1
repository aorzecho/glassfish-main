LIST-HTTP-LISTENER(1)                                    LIST-HTTP-LISTENER(1)

名前
       list-http-listeners - 既存のネットワーク・リスナーを一覧表示します

概要
           list-http-listeners [--help]
           [target]

説明
       list-http-listenersサブコマンドは、既存のネットワーク・リスナーを一覧表示します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           一覧表示する内容を、指定したターゲットのネットワーク・リスナーに制限します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスのネットワーク・リスナーを一覧表示します。この値がデフォルト値です。

           configuration-name
               指定した構成のネットワーク・リスナーを一覧表示します。

           cluster-name
               指定したクラスタ内のすべてのサーバー・インスタンスのネットワーク・リスナーを一覧表示します。

           instance-name
               指定したサーバー・インスタンスのネットワーク・リスナーを一覧表示します。

例
       例 1, ネットワーク・リスナーの一覧表示
           次のコマンドは、サーバー・インスタンスのすべてのネットワーク・リスナーを一覧表示します。

               asadmin> list-http-listeners
               http-listener-1
               http-listener-2
               admin-listener
               Command list-http-listeners executed successfully.

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-http-listener(1), delete-http-listener(1)

       asadmin(1M)

                                 2011年2月11日           LIST-HTTP-LISTENER(1)
