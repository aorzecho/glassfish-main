CREATE-PROTOCOL-FI(1)                                    CREATE-PROTOCOL-FI(1)

NOME
       create-protocol-filter - aggiunge un nuovo filtro di protocollo

SINTASSI
           create-protocol-filter [--help]
           --protocol protocol-name
           --classname class-name
           [--target server]
           protocol-filter-name

DESCRIZIONE
       Il comando secondario create-protocol-filter crea un filtro di
       protocollo per un protocollo. Questo comando secondario è supportato
       solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --protocol
           Nome del protocollo associato.

       --classname
           Nome completo della classe Java che implementa il filtro di
           protocollo.

       --target
           Crea il filtro di protocollo solo nella destinazione specificata. I
           valori validi sono i seguenti:

           server
               Crea il filtro di protocollo nell'istanza server predefinita.
               Questo è il valore predefinito.

           configuration-name
               Crea il filtro di protocollo nella configurazione specifica.

           cluster-name
               Crea il filtro di protocollo di tutte le istanze server nel
               cluster specificato.

           standalone-instance-name
               Crea il filtro di protocollo nell'istanza server standalone
               specificata.

OPERANDI
       protocol-filter-name
           Nome del filtro di protocollo.

ESEMPI
       Esempio 1, Creazione di filtro di protocollo
           Il seguente comando crea un filtro di protocollo denominato
           http1-filter:

               asadmin> create-protocol-filter --protocol http1
               --classname com.company22.MyProtocolFilter http1-filter
               Command create-protocol-filter executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       delete-protocol-filter(1), list-protocol-filters(1), create-protocol(1)

       asadmin(1M)

                                11 ottobre 2010          CREATE-PROTOCOL-FI(1)
