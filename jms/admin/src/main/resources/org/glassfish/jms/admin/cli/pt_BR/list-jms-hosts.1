LIST-JMS-HOSTS(1)                                            LIST-JMS-HOSTS(1)

NOME
       list-jms-hosts - lista os hosts JMS existentes

SINOPSE
           list-jms-hosts [--help]
           [--target target]

DESCRIçãO
       O subcomando list-jms-hosts lista os hosts Java Message Service (JMS)
       existentes para o serviço JMS.

       Esse subcomando só é suportado no modo remoto. Os subcomandos asadmin
       remotos requerem um servidor de administração de domínios (DAS) esteja
       em execução.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Restringe a listagem aos host JMS para um destino especificado. Os
           valores válidos são:

           servidor
               Lista os hosts JMS para a instância de servidor default. Este é
               o valor default.

           configuration-name
               Lista os hosts JMS para a configuração especificada.

           cluster-name
               Lista aos host JMS para todas as instâncias de servidor no
               cluster especificado.

           instance-name
               Lista os hosts JMS para a instância de servidor especificada.

EXEMPLOS
       Exemplo 1, Listando todos os hosts JMS
           O subcomando a seguir lista os hosts para o serviço JMS.

               asadmin> list-jms-hosts server-config
               default_JMS_host
               MyNewHost
               Command list-jms-hosts executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro ao executar o subcomando

CONSULTE TAMBéM
       create-jms-host(1), delete-jms-host(1), jms-ping(1)

       asadmin(1M)

                             22 de outubro de 2010           LIST-JMS-HOSTS(1)
