DISABLE-HTTP-LB-S(1)                                      DISABLE-HTTP-LB-S(1)

NOME
       disable-http-lb-server - desativa um servidor ou cluster gerenciado por
       um balanceador de carga

SINOPSE
           disable-http-lb-server [--help] [--timeout 30]
            target

DESCRIçãO
       O subcomando disable-http-lb-server desativa um servidor standalone ou
       um cluster de servidores para o balanceador de carga. A instância do
       servidor ou cluster desativado ficará off-line para balanceamento de
       carga com impacto mínimo para os usuários.

       Uma vez que o destino tenha sido desativado e as alterações aplicadas
       ao balanceador de carga, o balanceador de carga interrompe a designação
       de novas solicitações ao destino. Solicitações de sessões com sessões
       criadas antes da desativação do destino continuam a ser designadas a
       esse destino até que o time-out seja atingido. Este processo é
       conhecido como desativação.

       As alterações são automaticamente aplicadas ao balanceador de carga.
       Também é possível exportar manualmente a configuração usando
       export-http-lb-config e copiando-a para o balanceador de carga.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --timeout
           O time-out (em minutos) para aguardar antes de desativar o destino
           especificado. Esse tempo permite o shutdown normal (suspensão) do
           destino especificado. O valor default é 30 minutos. O valor mínimo
           é 1 minuto.

OPERANDOS
       target
           Este operando especifica as instâncias de servidor e clusters que
           serão desativados. Os valores válidos são:

           ·   cluster_name- O nome de um cluster de destino.

           ·   instance_name - O nome de uma instância do servidor de destino.

EXEMPLOS
       Exemplo 1, Desativando um cluster para o balanceador de carga
           Esse exemplo desativa o balanceamento de carga para um cluster
           chamado mycluster.

               asadmin> disable-http-lb-server mycluster

               Command disable-http-lb-server executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro ao executar o subcomando

CONSULTE TAMBéM
       create-http-lb-ref(1), enable-http-lb-server(1)

       asadmin(1M)

                             26 de agosto de 2010         DISABLE-HTTP-LB-S(1)
