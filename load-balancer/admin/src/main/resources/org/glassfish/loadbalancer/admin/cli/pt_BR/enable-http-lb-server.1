ENABLE-HTTP-LB-SE(1)                                      ENABLE-HTTP-LB-SE(1)

NOME
       enable-http-lb-server - ativa um servidor ou cluster desativado
       anteriormente gerenciado por um balanceador de carga

SINOPSE
           enable-http-lb-server [--help] target

DESCRIçãO
       O subcomando enable-http-lb-server ativa uma instância standalone do
       servidor ou cluster para o balanceador de carga. Por default, o
       balanceador de carga é ativado para instância e clusters.

           Nota
           ┌──────────────────────────────────────┐
           │Observação                            │
           │                                      │
           │           Este subcomando pode ser   │
           │           aplicado apenas ao Oracle  │
           │           GlassFish Server. Esse     │
           │           subcomando não é aplicável │
           │           ao GlassFish Server Open   │
           │           Source Edition.            │
           └──────────────────────────────────────┘

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

OPERANDOS
       target
           Esse operando especifica quais instâncias de servidor e clusters
           devem ser ativados. Os valores válidos são:

           ·   cluster_name- O nome de um cluster de destino.

           ·   instance_name - O nome de uma instância do servidor de destino.

EXEMPLOS
       Exemplo 1, Ativando um cluster para o balanceador de carga
           Este exemplo permite o balanceamento de cargas para um cluster
           denominado de mycluster.

               asadmin> enable-http-lb-server mycluster

               Command enable-http-lb-server executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro ao executar o subcomando

CONSULTE TAMBéM
       create-http-lb-ref(1), disable-http-lb-server(1)

       asadmin(1M)

                             26 de agosto de 2010         ENABLE-HTTP-LB-SE(1)
