DELETE-CONNECTOR-R(1)                                    DELETE-CONNECTOR-R(1)

NOME
       delete-connector-resource - remove o recurso do conector com o nome
       JNDI especificado

SINOPSE
           delete-connector-resource [--help] [--target target] jndi_name

DESCRIçãO
       O subcomando delete-connector-resource remove o recurso conector com o
       nome JNDI especificado.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Essa opção especifica o destino a partir do qual você deseja
           remover o recurso conector. Os destinos válidos são:

               Nota
               ┌────────────────────────────────────────┐
               │Observação Os recursos sempre são       │
               │criados para um domínio como um todo,   │
               │mas só estarão ativos para destinos     │
               │para os quais <resource-ref> tiver sido │
               │criado usando a opção --target durante  │
               │a criação do recurso. Isso significa    │
               │que a deleção de um recurso só deleta o │
               │elemento                                │
               │ <resource-ref> para o --target         │
               │especificado e não deleta o recurso do  │
               │domínio como um todo, a não ser que o   │
               │domain seja especificado como o         │
               │--domain para a deleção.                │
               └────────────────────────────────────────┘

           server
               Deleta o recurso conector a partir da instância de servidor
               default. Este é o valor default.

           domain
               Deleta o recurso conector a partir do domínio.

           cluster_name
               Deleta o recurso conector a partir de cada instância do
               servidor no cluster.

           instance_name
               Deleta o recurso conector a partir da instância de servidor
               especificada.

OPERANDOS
       jndi_name
           O nome JNDI desse recurso conector.

EXEMPLOS
       Exemplo 1, Deletando um recurso conector
           Este exemplo deleta um recurso do conector denominado
           jms/qConnFactory.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully

       Exemplo 2, Utilizando o subcomando delete-connector-resource
           Este exemplo mostra o uso desse subcomando.

               asadmin> delete-connector-resource jms/qConnFactory
               Command delete-connector-resource executed successfully
           Onde jms/qConnFactory é o recurso do conector que será removido.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro ao executar o subcomando

CONSULTE TAMBéM
       create-connector-resource(1), list-connector-resources(1)

       asadmin(1M)

                            11 de fevereiro de 2011      DELETE-CONNECTOR-R(1)
