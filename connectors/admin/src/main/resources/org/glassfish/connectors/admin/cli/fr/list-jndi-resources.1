LIST-JNDI-RESOURCE(1)                                    LIST-JNDI-RESOURCE(1)

NOM
       list-jndi-resources - répertorie toutes les ressources JNDI existantes

SYNOPSIS
           list-jndi-resources [--help] [target]

DESCRIPTION
       La sous-commande list-jndi-resources identifie toutes les
       ressources JNDI existantes.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Affiche le texte d'aide de la sous-commande.

OPÉRANDES
       target
           Cible pour laquelle les ressources JNDI doivent être répertoriées.
           Les valeurs valides sont les suivantes :

           server
               Répertorie les ressources JNDI sur l'instance de serveur par
               défaut. Ceci est la valeur par défaut.

           configuration-name
               Répertorie les ressources JNDI pour la configuration spécifiée.

           cluster-name
               Répertorie les ressources JNDI sur toutes les instances de
               serveur du cluster spécifié.

           instance-name
               Répertorie les ressources JNDI sur une instance de serveur
               spécifiée.

EXEMPLES
       Exemple 1, Liste des ressources JNDI
           Cet exemple répertorie les ressources JNDI sur l'instance de
           serveur par défaut.

               asadmin> list-jndi-resources
               jndi_resource1
               jndi_resource2
               jndi_resource3
               Command list-jndi-resources executed successfully

STATUT DE SORTIE
       0
           sous-commande correctement exécutée

       1
           erreur lors de l'exécution de la sous-commande

VOIR AUSSI
       create-jndi-resource(1), delete-jndi-resource(1)

       asadmin(1M)

                                 21 juin 2011            LIST-JNDI-RESOURCE(1)
