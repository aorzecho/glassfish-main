LIST-SECURE-ADMIN()                                        LIST-SECURE-ADMIN()

NOME
       list-secure-admin-internal-users - elenca i nomi utente che il DAS e le
       istanze di GlassFish Server DAS utilizzano per eseguire
       l'autenticazione reciproca e per autorizzare le operazioni di
       amministrazione.

SINTASSI
           list-secure-admin-internal-users
           [--help]

DESCRIZIONE
       Il comando secondario list-secure-admin-internal-users elenca i nomi
       utente che il DAS e le istanze di GlassFish Server DAS utilizzano per
       eseguire l'autenticazione reciproca e per autorizzare le operazioni di
       amministrazione.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

ESEMPI
       Esempio 1, Elenca il nome utente per l'amministrazione sicura
           L'esempio seguente elenca i nomi utente che il DAS e le istanze di
           GlassFish Server DAS utilizzano per eseguire l'autenticazione
           reciproca e per autorizzare le operazioni di amministrazione.

               asadmin> list-secure-admin-internal-users

               Command list-secure-admin-internal-users executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       enable-secure-admin(1)

       enable-secure-admin-internal-user(1)

       asadmin(1M)

                                                           LIST-SECURE-ADMIN()
