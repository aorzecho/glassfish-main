DELETE-RESOURCE-RE(1)                                    DELETE-RESOURCE-RE(1)

參考名
       delete-resource-ref - 移除資源的參照

概要
           delete-resource-ref [--help] [--target target] reference_name

說明
       delete-resource-ref 子命令會從叢集或非叢集的伺服器實例移除資源
       (例如，JDBC 資源) 的參照。這會導致從目標實例或叢集的 JNDI
       樹狀結構移除資源。

       這個子命令不需要目標實例或構成叢集的實例，更不需執行它們，便可成功執行。如果一個或多個實例無法使用，它們將在下次啟動時不再載入
       JNDI 樹狀結構中的資源。

       移除參照不會導致資源從網域移除。此資源只能使用該資源的 delete
       子命令移除 (例如，delete-jdbc-resource)。

       只有遠端模式支援這個子命令。

選項
       --help, -?
           顯示子命令的說明文字。

       --target
           指定您要從中移除資源參照的目標。有效值是

           server
               從預設伺服器實例 server 移除資源參照，且此為預設值。

           cluster_name
               從叢集中的每個伺服器實例移除資源參照。

           instance_name
               從指定的非叢集的伺服器實例移除資源參照。

運算元
       reference_name
           資源的名稱或 JNDI 名稱。

範例
       範例 1, 移除資源的參照
           此範例會移除叢集 cluster1 上 JMS 目標資源 jms/Topic 的參照。

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

結束狀態
       0
           子命令執行成功

       1
           執行子命令時發生錯誤

另請參閱
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

                              2011 年 6 月 20 日         DELETE-RESOURCE-RE(1)
