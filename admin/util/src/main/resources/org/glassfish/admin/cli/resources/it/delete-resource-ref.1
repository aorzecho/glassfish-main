DELETE-RESOURCE-RE(1)                                    DELETE-RESOURCE-RE(1)

NOME
       delete-resource-ref - rimuove un riferimento a una risorsa

SINTASSI
           delete-resource-ref [--help] [--target target] reference_name

DESCRIZIONE
       Il comando secondario delete-resource-ref rimuove un riferimento a una
       risorsa (ad esempio una risorsa JDBC) da un cluster o da un'istanza
       server non inclusa in un cluster. Questo comporta la rimozione della
       risorsa dalla struttura JNDI dell'istanza o del cluster di
       destinazione.

       Non è necessario che l'istanza di destinazione o le istanze che
       compongono il cluster siano in esecuzione perché questo comando
       secondario venga eseguito correttamente. Se una o più istanze non sono
       disponibili, non caricheranno più la risorsa nella struttura JNDI
       all'avvio successivo.

       La rimozione del riferimento non comporta la rimozione della risorsa
       dal dominio. La risorsa viene rimossa solo con il comando secondario
       delete corrispondente (ad esempio delete-jdbc-resource).

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --target
           Specifica la destinazione da cui viene rimosso il riferimento alla
           risorsa. I valori validi sono

           server
               Rimuove il riferimento alla risorsa dall'istanza server
               predefinita
                server ed è il valore predefinito.

           cluster_name
               Rimuove il riferimento alla risorsa da tutte le istanze server
               del cluster.

           instance_name
               Rimuove il riferimento alla risorsa dall'istanza server
               denominata non inserita in un cluster.

OPERANDI
       reference_name
           Nome o nome JNDI della risorsa.

ESEMPI
       Esempio 1, Rimozione di un riferimento a una risorsa
           L'esempio riportato di seguito rimuove un riferimento alla risorsa
           di destinazione JMS jms/Topic nel cluster cluster1.

               asadmin> delete-resource-ref --target cluster1 jms/Topic
               resource-ref jms/Topic deleted successfully.
               Command delete-resource-ref executed successfully.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-resource-ref(1), list-resource-refs(1)

       asadmin(1M)

                                20 giugno 2011           DELETE-RESOURCE-RE(1)
