LIST-AUDIT-MODULES(1)                                    LIST-AUDIT-MODULES(1)

名前
       list-audit-modules - すべての監査モジュールを取得して表示します

概要
           list-audit-modules [--help] [target]

説明
       list-audit-modulesサブコマンドはすべての監査モジュールを一覧表示します。このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       target
           監査モジュールを一覧表示するターゲットを指定します。有効な値は次のとおりです:

           server
               デフォルトのサーバー・インスタンスserverの監査モジュールを一覧表示します。これはデフォルト値です。

           configuration_name
               名前付き構成の監査モジュールを一覧表示します。

           cluster_name
               クラスタ内のすべてのサーバー・インスタンスの監査モジュールを一覧表示します。

           instance_name
               特定のサーバー・インスタンスの監査モジュールを一覧表示します。

例
       例 1, 監査モジュールの一覧表示

               asadmin> list-audit-modules
               sampleAuditModule1
               sampleAuditModule2
               Command list-audit-modules executed successfully

終了ステータス
       0
           コマンドは正常に実行されました

       1
           コマンドの実行時にエラーが発生しました

関連項目
       create-audit-module(1), delete-audit-module(1)

       asadmin(1M)

                                2010年12月22日           LIST-AUDIT-MODULES(1)
