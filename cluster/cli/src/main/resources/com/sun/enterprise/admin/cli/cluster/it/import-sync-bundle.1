IMPORT-SYNC-BUNDLE(1)                                    IMPORT-SYNC-BUNDLE(1)

NOME
       import-sync-bundle - importa i dati di configurazione di un'istanza in
       cluster o standalone da un file di archivio

SINTASSI
           import-sync-bundle [--help]
           --instance instance-name
           [--nodedir node-dir] [--node node-name]
           file-name

DESCRIZIONE
       Il comando secondario import-sync-bundle importa i dati di
       configurazione di un'istanza in cluster o standalone da un file di
       archivio creato dal comando secondario export-sync-bundle(1).

       È necessario eseguire tale comando secondario nell'host in cui risiede
       l'istanza. Per contattare il server di amministrazione del dominio
       (DAS), tale comando secondario richiede il nome dell'host in cui il DAS
       è in esecuzione. Se viene utilizzata una porta non predefinita per
       l'amministrazione, tale comando secondario richiede inoltre il numero
       di porta. È necessario fornire tale informazione tramite le opzioni
       --host e --port della utility asadmin(1M).

       L'importazione dei dati di configurazione dell'istanza trasferisce i
       dati a un host per un'istanza, senza necessità che questa sia in grado
       di comunicare con il DAS. L'importazione di dati di configurazione di
       un'istanza è solitamente richiesta per i seguenti motivi:

       ·   Per ristabilire l'istanza dopo un aggiornamento

       ·   Per sincronizzare manualmente l'istanza con il server di
           amministrazione del dominio (DAS) quando l'istanza non riesce a
           contattare il DAS

       Il comando secondario importa un dato di configurazione istanza
       mediante le operazioni seguenti:

       ·   Creazione o aggiornamento di directory e file dell'istanza

       ·   Tentativo di registrazione dell'istanza con il DAS

       Se il tentativo di registrare l'istanza con il DAS fallisce, il comando
       secondario riesce. Il comando secondario mostra invece un'avvertenza
       che indica che il tentativo non è riuscito. L'avvertenza contiene il
       comando da eseguire per registrare l'istanza con il DAS.

       Il comando secondario import-sync-bundle non contatta il DAS per
       determinare il nodo in cui risiede l'istanza. Se il nodo non è
       specificato come opzione del comando secondario, quest'ultimo determina
       il nodo dalla configurazione del DAS nel file di archivio.

       Questo comando secondario è supportato solo in modalità locale.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --instance
           Istanza per la quale viene importato il dato di configurazione.
           L'istanza deve esistere già nella configurazione DAS. Il file di
           archivio dal quale viene importato il dato deve contenere i dati
           per l'istanza specificata.

       --nodedir
           Directory che contiene la directory del nodo dell'istanza. I file
           dell'istanza sono memorizzati nella directory del nodo
           dell'istanza. Il valore predefinito è as-install/nodes.

       --node
           Il nodo in cui risiede l'istanza. Se l'opzione è omessa, il comando
           secondario determina il nodo dalla configurazione DAS nel file di
           archivio.

OPERANDI
       file-name
           Il nome del file, incluso il percorso che contiene il file di
           archivio da importare. Questo operando è obbligatorio.

ESEMPI
       Esempio 1, Importazione di dati di configurazione per un'istanza in
       cluster
           Tale esempio importa la configurazione per l'istanza in cluster
           ymli2 nel nodo sj02 dal file di archivio
           /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip.

           Il comando è eseguito nell'host sj02, ovvero in quello
           rappresentato dal nodo sj02. Il DAS è in esecuzione nell'host sr04
           e utilizza la porta HTTP predefinita per l'amministrazione.

               sj02# asadmin --host sr04 import-sync-bundle --node sj02 --instance ymli2
               /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip
               Command import-sync-bundle executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       export-sync-bundle(1)

       asadmin(1M)

                                7 dicembre 2010          IMPORT-SYNC-BUNDLE(1)
