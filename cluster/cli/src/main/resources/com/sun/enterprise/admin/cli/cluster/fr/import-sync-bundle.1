IMPORT-SYNC-BUNDLE(1)                                    IMPORT-SYNC-BUNDLE(1)

NOM
       import-sync-bundle - importe les données de configuration d'une
       instance incluse dans un cluster ou d'une instance autonome à partir
       d'un fichier d'archive

SYNOPSIS
           import-sync-bundle [--help]
           --instance instance-name
           [--nodedir node-dir] [--node node-name]
           file-name

DESCRIPTION
       La sous-commande import-sync-bundle permet d'importer les données de
       configuration d'une instance incluse dans un cluster ou d'une instance
       autonome à partir d'un fichier d'archive créé par la sous-commande
       export-sync-bundle(1).

       Vous devez exécuter cette sous-commande à partir de l'hôte sur lequel
       l'instance réside. Pour contacter le serveur d'administration de
       domaine (DAS), cette sous-commande requiert le nom de l'hôte sur lequel
       le serveur DAS est en cours d'exécution. Si un port autre que le port
       par défaut est utilisé pour l'administration, cette sous-commande
       requiert également le numéro de port. Vous devez fournir ces
       informations via les options --host et --port de l'utilitaire
       asadmin(1M).

       Le fait d'importer les données de configuration d'une instance les
       transfère vers un hôte, sans que l'instance ne doive communiquer avec
       le serveur DAS. L'import des données de configuration d'une instance
       est généralement nécessaire pour les raisons suivantes :

       ·   Pour rétablir l'instance après une mise à niveau

       ·   Pour synchroniser l'instance manuellement avec le serveur
           d'administration de domaine (DAS) lorsque l'instance ne peut pas
           contacter le serveur DAS

       La sous-commande importe les données de configuration d'une instance en
       effectuant les opérations suivantes :

       ·   Création ou mise à jour des fichiers et répertoires de l'instance

       ·   Tentative d'inscription de l'instance auprès du serveur DAS

       En cas d'échec de la tentative d'inscription de l'instance auprès du
       serveur DAS, la sous-commande n'échoue pas. Au lieu de cela, la
       sous-commande affiche un message d'avertissement indiquant que la
       tentative a échoué. L'avertissement contient la commande permettant
       d'inscrire l'instance auprès du serveur DAS.

       La sous-commande import-sync-bundle ne contacte pas le serveur DAS pour
       déterminer le noeud sur lequel l'instance réside. Si le noeud n'est pas
       spécifié comme une option de la sous-commande, la sous-commande
       détermine le noeud de la configuration DAS dans le fichier d'archive.

       Cette sous-commande est prise en charge en mode local uniquement.

OPTIONS
       --help, -?
           Affiche le texte d'aide de la sous-commande.

       --instance
           Instance pour laquelle les données de configuration sont en cours
           d'import. L'instance doit déjà exister dans la configuration du
           serveur DAS. Le fichier d'archive à partir duquel les données sont
           en cours d'import doit contenir des données de l'instance indiquée.

       --nodedir
           Répertoire contenant le répertoire de noeuds de l'instance. Les
           fichiers de l'instance sont stockés dans le répertoire de noeud de
           l'instance. La valeur par défaut est as-install/nodes.

       --node
           Noeud sur lequel l'instance réside. Si cette option est omise, la
           sous-commande détermine le noeud à partir de la configuration du
           serveur DAS dans le fichier d'archive.

OPÉRANDES
       file-name
           Nom du fichier, y compris le chemin d'accès, qui contient le
           fichier d'archive à importer. Cet opérande est requis.

EXEMPLES
       Exemple 1, Import de données de configuration pour une instance incluse
       dans un cluster
           Cet exemple importe la configuration de l'instance incluse dans un
           cluster ymli2 sur le noeud sj02 à partir du fichier d'archive
           /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip.

           La commande est exécutée sur l'hôte sj02, que le noeud sj02
           représente. Le serveur DAS est exécuté sur l'hôte sr04 et utilise
           le port HTTP par défaut pour l'administration.

               sj02# asadmin --host sr04 import-sync-bundle --node sj02 --instance ymli2
               /export/glassfish3/glassfish/domains/domain1/sync/ymlcluster-sync-bundle.zip
               Command import-sync-bundle executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       export-sync-bundle(1)

       asadmin(1M)

                                  7 déc. 2010            IMPORT-SYNC-BUNDLE(1)
