LIST-NODES-DCOM(1)                                          LIST-NODES-DCOM(1)

NOME
       list-nodes-dcom - lista todos os nós do GlassFish Server que suportam a
       comunicação pelo DCOM em um domínio

SINOPSE
           list-nodes-dcom [--help] [--long={false|true}]

DESCRIçãO
       O subcomando list-nodes-dcom lista todos os nós do GlassFish Server que
       suportam a comunicação pelo protocolo remoto Distributed Component
       Object Model (DCOM). O protocolo DCOM só está disponível nos sistemas
       Windows.

           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           Para listar todos os nós em  │
           │           um domínio,                  │
           │           independentemente do tipo do │
           │           nó, execute o subcomando     │
           │           list-nodes(1).               │
           └────────────────────────────────────────┘

       Por default, o subcomando exibe as seguintes informações para cada nó
       listado:

       ·   O nome do nó

       ·   O tipo de nó, que é DCOM

       ·   O nome do host cujo o nó representa

       A opção --long do subcomando especifica os nós que são listados no
       formato longo. No formato longo, as seguintes informações adicionais
       sobre cada nó são exibidas;

       ·   O caminho para o pai do diretório-base de instalação do no host que
           o nó representa.

       ·   Uma lista separada por vírgulas com os nomes de instâncias do
           GlassFish Server que residem no nó.

       Se a opção --terse do utilitário asadmin(1M) for true e a opção --long
       do subcomando for false, o subcomando listará o nome de cada nó.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --long, -l
           Especifica se os nós são listados no formato longo.

           Os valores possíveis são os seguintes:

           true
               Os nós são listados no formato longo.

           false
               Os nós são listados no formato curto (default).

EXEMPLOS
       Exemplo 1, Listando Nós do GlassFish Server
           Este exemplo exibe o nome, tipo e host de todos os nós do GlassFish
           Server que suportam a comunicação por meio do DCOM em um domínio.

               asadmin> list-nodes-dcom
               xkyd  DCOM  xkyd.example.com
               wpmdl2  DCOM  wpmdl2.example.com
               wpmdl1  DCOM  wpmdl1.example.com
               Command list-nodes-dcom executed successfully.

       Exemplo 2, Listando Somente os Nomes dos Nós do GlassFish Server
           Este exemplo usa a opção --terse do utilitário asadmin para listar
           só os nomes dos nós do GlassFish Server que suportam a comunicação
           por meio do DCOM em um domínio.

               asadmin> list-nodes-dcom --terse=true
               xkyd
               wpmdl2
               wpmdl1

       Exemplo 3, Listando Nós do GlassFish Server no Formato Longo
           Este exemplo lista os nós do GlassFish Server que suportam a
           comunicação por meio do DCOM em um domínio no formato longo.

               asadmin> list-nodes-dcom --long=true
               NODE NAME    TYPE   NODE HOST            INSTALL DIRECTORY   REFERENCED BY
               xkyd         DCOM   xkyd.example.com     C:\glassfish3
               wpmdl2       DCOM   wpmdl2.example.com   C:\glassfish3       wdi2
               wpmdl1       DCOM   wpmdl1.example.com   C:\glassfish3       wdi1
               Command list-nodes-dcom executed successfully.

STATUS DE SAíDA
       0
           comando executado com sucesso

       1
           erro ao executar o comando

CONSULTE TAMBéM
       create-node-config(1), create-node-dcom(1), create-node-ssh(1),
       delete-node-config(1), delete-node-dcom(1), delete-node-ssh(1),
       list-nodes(1), list-nodes-config(1), list-nodes-dcom(1)

       asadmin(1M)

                            05 de dezembro de 2011          LIST-NODES-DCOM(1)
