DELETE-CONFIG(1)                                              DELETE-CONFIG(1)

NOM
       delete-config - supprime une configuration nommée existante

SYNOPSIS
           delete-config [--help] configuration-name

DESCRIPTION
       La sous-commande delete-config supprime une configuration nommée
       existante de la configuration du serveur d'administration de
       domaine (DAS). Vous pouvez supprimer une configuration uniquement si
       aucune instance et aucun cluster GlassFish Server n'y fait référence.
       Une configuration autonome est automatiquement supprimée lorsque
       l'instance ou le cluster qui y fait référence est supprimé. Vous ne
       pouvez pas supprimer la configuration default-config copiée pour créer
       des configurations autonomes.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Affiche le texte d'aide de la sous-commande.

OPÉRANDES
       configuration-name
           Nom de la configuration que vous supprimez.

EXEMPLES
       Exemple 1, Suppression d'une configuration nommée
           Dans cet exemple, la configuration nommée pmdconfig est supprimée.

               asadmin> delete-config pmdconfig

               Command delete-config executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       copy-config(1), list-configs(1)

       asadmin(1M)

       configuration(5ASC)

                                  7 déc. 2010                 DELETE-CONFIG(1)
