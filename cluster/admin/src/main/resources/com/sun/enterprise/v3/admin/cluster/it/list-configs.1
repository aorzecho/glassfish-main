LIST-CONFIGS(1)                                                LIST-CONFIGS(1)

NOME
       list-configs - elenca le configurazioni denominate

SINTASSI
           list-configs [--help] [target]

DESCRIZIONE
       Il comando secondario list—configs elenca le configurazioni specificate
       nella configurazione del server di amministrazione del dominio (DAS).
       L'elenco può essere filtrato per cluster, istanza o configurazione
       specificata.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

OPERANDI
       target
           Filtra l'elenco delle configurazioni. I valori validi sono i
           seguenti:

           domain
               Elenca tutte le configurazioni denominate nel dominio corrente.

           cluster-name
               Elenca la configurazione denominata che definisce la
               configurazione delle istanze nel cluster specificato.

           instance-name
               Elenca la configurazione denominata che definisce la
               configurazione dell'istanza specificata.

           configuration-name
               Elenca la configurazione denominata specificata. Utilizzare
               questa opzione per determinare se esiste una configurazione
               denominata.

ESEMPI
       Esempio 1, Visualizzazione dell'elenco di tutte le configurazioni
       denominate in un dominio
           In questo esempio vengono elencate tutte le configurazioni
           denominate nel dominio corrente.

               asadmin> list-configs
               server-config
               default-config
               pmdclust-config
               pmdsharedconfig
               pmdcpinst-config
               ymlclust-config
               il1-config
               il2-config

               Command list-configs executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       copy-config(1), delete-config(1)

       asadmin(1M)

       configurazione(5ASC)

                               20 dicembre 2010                LIST-CONFIGS(1)
