LIST-NODES-CONFIG(1)                                      LIST-NODES-CONFIG(1)

NOME
       list-nodes-config - elenca tutti i nodi GlassFish Server che non
       supportano la comunicazione remota in un dominio

SINTASSI
           list-nodes-config [--help] [--long={false|true}]

DESCRIZIONE
       Il comando secondario list-nodes-config consente di elencare tutti i
       nodi GlassFish Server che non supportano la comunicazione remota in un
       dominio.

           Nota
           ┌────────────────────────────────────────┐
           │Nota                                    │
           │                                        │
           │           Per elencare tutti i nodi in │
           │           un dominio,                  │
           │           indipendentemente dal tipo   │
           │           di nodo, eseguire il comando │
           │           secondario list-nodes(1).    │
           └────────────────────────────────────────┘

       Per impostazione predefinita, il comando secondario mostra le
       informazioni seguenti per ciascun nodo elencato:

       ·   Nome del nodo

       ·   Il tipo di nodo, che è CONFIG

       ·   Nome dell'host che il nodo rappresenta

       L'opzione --long del comando secondario specifica se i nodi sono
       elencati in un formato esteso. In formato esteso vengono visualizzate
       le seguenti informazioni aggiuntive relative a ciascun nodo:

       ·   Percorso del livello superiore della directory base di
           installazione di GlassFish Server nell'host rappresentato dal nodo

       ·   L'elenco separato da virgole dei nomi delle istanze di GlassFish
           Server che risiedono nel nodo

       Se l'opzione --terse della utilità asadmin(1M) è true e l'opzione
       --long del comando secondario è false, il comando secondario elenca
       solo il nome di ogni nodo.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --long, -l
           Specifica se i nodi sono elencati in un formato esteso.

           I possibili valori sono i seguenti:

           true
               I nodi sono elencati in un formato esteso.

           false
               I nodi sono elencati in un formato corto (opzione predefinita).

ESEMPI
       Esempio 1, Visualizzazione dell'elenco dei nodi GlassFish Server
           In questo esempio vengono visualizzati nome, tipo e host per tutti
           i nodi GlassFish Server che non supportano la comunicazione remota
           nel dominio domain1.

               asadmin> list-nodes-config
               localhost-domain1  CONFIG  localhost
               devnode  CONFIG  localhost
               Command list-nodes-config executed successfully.

       Esempio 2, Visualizzazione dell'elenco solo dei nomi dei nodi GlassFish
       Server
           In questo esempio viene utilizzata l'opzione --terse della utility
           asadmin per elencare soltanto i nomi dei nodi GlassFish Server che
           non supportano la comunicazione remota nel dominio domain1.

               asadmin> list-nodes-config --terse=true
               localhost-domain1
               devnode

       Esempio 3, Visualizzazione dell'elenco di nodi GlassFish Server in
       formato esteso
           In questo esempio vengono elencati i nodi GlassFish Server che non
           supportano la comunicazione remota nel dominio domain1 in formato
           esteso.

               asadmin> list-nodes-config --long=true
               NODE NAME           TYPE     NODE HOST   INSTALL DIRECTORY    REFERENCED BY
               localhost-domain1   CONFIG   localhost   /export/glassfish3
               devnode             CONFIG   localhost   /export/glassfish3   pmdsa1
               Command list-nodes-config executed successfully.

STATO DI USCITA
       0
           esecuzione del comando riuscita

       1
           errore durante l'esecuzione del comando

VEDERE ANCHE
       create-node-config(1), create-node-dcom(1), create-node-ssh(1),
       delete-node-config(1), delete-node-dcom(1), delete-node-ssh(1),
       list-nodes(1), list-nodes-dcom(1), list-nodes-ssh(1)

       asadmin(1M)

                                8 dicembre 2011           LIST-NODES-CONFIG(1)
