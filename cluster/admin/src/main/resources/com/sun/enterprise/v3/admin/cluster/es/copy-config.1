COPY-CONFIG(1)                                                  COPY-CONFIG(1)

NOMBRE DE REFERENCIA
       copy-config - Copia una configuración existente especificada para crear
       otra configuración.

SINOPSIS
           copy-config [--help]
           [--systemproperties (name=value)[:name=value]*]
           source-configuration-name destination-configuration-name

DESCRIPCIÓN
       El subcomando copy-config crea una configuración especificada en la
       configuración del servidor de administración de dominio (DAS) mediante
       la copia de una configuración existente. La nueva configuración es
       idéntica a la configuración copiada, excepto para las propiedades que
       se especifiquen en la opción --systemproperties.

       La configuración default-config es la configuración que se copia cuando
       se crea una instancia de servidor o cluster independientes.

       Este subcomando sólo está soportado en modo remoto.

OPCIONES
       --help, -?
           Muestra el texto de ayuda para el subcomando.

       --systemproperties
           Pares de nombre-valor de atributo opcionales para la configuración.
           Estas propiedades sustituyen las preferencias de puerto de la
           configuración.

           Están disponibles las siguientes propiedades:

           ASADMIN_LISTENER_PORT
               Esta propiedad especifica el número de puerto del puerto HTTP o
               HTTPS puerto a través del cual el DAS se conecta a la instancia
               para administrar la instancia. Los valores válidos son 1-65535.
               En sistemas UNIX, la creación de sockets de escucha en los
               puertos 1-1024 requiere privilegios de superusuario.

           HTTP_LISTENER_PORT
               Esta propiedad especifica el número del puerto que se utiliza
               para recibir las solicitudes HTTP. Los valores válidos son
               1-65535. En sistemas UNIX, la creación de sockets de escucha en
               los puertos 1-1024 requiere privilegios de superusuario.

           HTTP_SSL_LISTENER_PORT
               Esta propiedad especifica el número del puerto que se utiliza
               para recibir solicitudes HTTPS. Los valores válidos son
               1-65535. En sistemas UNIX, la creación de sockets de escucha en
               los puertos 1-1024 requiere privilegios de superusuario.

           IIOP_LISTENER_PORT
               Esta propiedad especifica el número del puerto que se utiliza
               para las conexiones IIOP. Los valores válidos son 1-65535. En
               sistemas UNIX, la creación de sockets de escucha en los puertos
               1-1024 requiere privilegios de superusuario.

           IIOP_SSL_LISTENER_PORT
               Esta propiedad especifica el número del puerto que se utiliza
               para conexiones IIOP seguras. Los valores válidos son 1-65535.
               En sistemas UNIX, la creación de sockets de escucha en los
               puertos 1-1024 requiere privilegios de superusuario.

           IIOP_SSL_MUTUALAUTH_PORT
               Esta propiedad especifica el número del puerto que se utiliza
               para conexiones IIOP seguras con autenticación de cliente. Los
               valores válidos son 1-65535. En sistemas UNIX, la creación de
               sockets de escucha en los puertos 1-1024 requiere privilegios
               de superusuario.

           JAVA_DEBUGGER_PORT
               Esta propiedad especifica el número del puerto que se utiliza
               para las conexiones con el depurador de Java Platform Debugger
               Architecture (JPDA)
               (http://java.sun.com/javase/technologies/core/toolsapis/jpda/).
               Los valores válidos son 1-65535. En sistemas UNIX, la creación
               de sockets de escucha en los puertos 1-1024 requiere
               privilegios de superusuario.

           JMS_PROVIDER_PORT
               Esta propiedad especifica el número de puerto del proveedor de
               Java Message Service. Los valores válidos son 1-65535. En
               sistemas UNIX, la creación de sockets de escucha en los puertos
               1-1024 requiere privilegios de superusuario.

           JMX_SYSTEM_CONNECTOR_PORT
               Esta propiedad especifica el número de puerto en el que escucha
               el conector JMX. Los valores válidos son 1-65535. En sistemas
               UNIX, la creación de sockets de escucha en los puertos 1-1024
               requiere privilegios de superusuario.

           OSGI_SHELL_TELNET_PORT
               Esta propiedad especifica el número del puerto utilizado para
               las conexiones con el Shell remoto Apache Felix
               (http://felix.apache.org/site/apache-felix-remote-shell.html).
               Este shell utiliza el servicio Felix shell para interactuar con
               el subsistema de administración de módulos OSGi. Los valores
               válidos son 1-65535. En sistemas UNIX, para la creación de
               sockets de recepción en los puertos 1-1024 se necesitan
               privilegios de superusuario.

OPERANDOS
       source-configuration-name
           Nombre de la configuración que se va a copiar.

       destination-configuration-name
           Nombre de la configuración que se va a crear mediante la copia de
           la configuración de origen.

           El nombre debe cumplir los siguientes requisitos:

           ·   El nombre sólo puede contener caracteres ASCII.

           ·   El nombre debe empezar por una letra, un número o un carácter
               de subrayado.

           ·   El nombre sólo puede contener los siguientes caracteres:

               ·   Letras en minúscula

               ·   Letras en mayúscula

               ·   Números

               ·   Guión

               ·   Punto

               ·   Subrayado

           ·   El nombre debe ser exclusivo en el dominio y no debe ser el
               nombre de otra configuración con nombre, otro cluster, una
               instancia de GlassFish Server ni otro nodo.

           ·   El nombre no debe ser domain, server ni otra palabra clave
               reservada para GlassFish Server.

EJEMPLOS
       Ejemplo 1, Copia de una Configuración
           En este ejemplo se copia la configuración default-config en la
           configuración pmdsaconfig, omitiendo los valores de configuración
           de los siguientes puertos:

           ·   Puerto de recepción HTTP

           ·   Puerto de recepción HTTPS

               asadmin> copy-config
               --systemproperties HTTP_LISTENER_PORT=2000:HTTP_SSL_LISTENER_PORT=3000
               default-config pmdsaconfig

               Command copy-config executed successfully.

ESTADO DE SALIDA
       0
           Comando ejecutado correctamente

       1
           Error al ejecutar el comando

CONSULTE TAMBIÉN
       delete-config(1), list-configs(1)

       Utilidad asadmin(1M)

                                  14 Jun 2011                   COPY-CONFIG(1)
