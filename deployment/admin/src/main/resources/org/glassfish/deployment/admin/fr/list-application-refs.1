LIST-APPLICATION-R(1)                                    LIST-APPLICATION-R(1)

NOM
       list-application-refs - répertorie les références d'application
       existantes

SYNOPSIS
           list-application-refs [--help] [--long={false|true}] [target]

DESCRIPTION
       La sous-commande list-application-refs répertorie toutes les références
       d'application dans un cluster ou une instance de serveur non incluse
       dans un cluster. Elle répertorie tous les modules déployés sur la cible
       spécifiée (par exemple, les applications J2EE, les modules Web et les
       modules EJB).

       Si plusieurs versions d'un module ou d'une application sont déployées,
       cette sous-commande répertorie toutes les versions. Pour afficher la
       liste des versions activées, définissez l'option --long sur true. Pour
       plus d'informations sur les versions des modules et des applications,
       reportez-vous à "Module and Application Versions" dans le manuel Oracle
       GlassFish Server Application Deployment Guide.

       Il n'est pas nécessaire que l'instance ou les instances cible formant
       le cluster soient en cours d'exécution ou disponibles pour que cette
       sous-commande réussisse.

       Cette sous-commande est prise en charge en mode distant uniquement.

OPTIONS
       --help, -?
           Affiche le texte d'aide de la sous-commande.

       --long
           Si l'option a la valeur true, affiche si chaque module ou
           application répertorié(e) est activé(e). La valeur par défaut est
           false.

OPÉRANDES
       target
           Cible pour laquelle vous répertoriez les références d'application.
           Les valeurs valides sont

           ·   server : spécifie l'instance de serveur par défaut comme cible.
               server est le nom de l'instance de serveur par défaut et la
               valeur par défaut.

           ·   cluster_name : spécifie un cluster particulier comme cible.

           ·   instance_name :spécifie une instance de serveur particulière en
               tant que cible.

EXEMPLES
       Exemple 1, Liste des références d'application
           L'exemple suivant répertorie les références d'application pour
           l'instance de serveur non incluse dans un cluster NewServer.

               asadmin> list-application-refs NewServer
               ClientSessionMDBApp
               MEjbApp
               __ejb_container_timer_app
               Command list-application-refs executed successfully.

STATUT DE SORTIE
       0
           commande correctement exécutée

       1
           erreur lors de l'exécution de la commande

VOIR AUSSI
       create-application-ref(1), delete-application-ref(1)

       asadmin(1M)

       Oracle GlassFish Server Application Deployment Guide

                                 22 sept. 2010           LIST-APPLICATION-R(1)
