REMOVE-LIBRARY(1)                                            REMOVE-LIBRARY(1)

名前
       remove-library - 1つ以上のライブラリJARファイルをGlassFish
       Serverから削除します

概要
           remove-library [--help] [--type={common|ext|app}]

           library-name [library-name ... ]

説明
       remove-libraryサブコマンドは、1つ以上のライブラリJARファイルをGlassFish
       Serverから削除します

       --typeオプションは、ライブラリ・タイプおよびライブラリを削除するGlassFish
       Serverディレクトリを指定します。

       library-nameオペランドは、削除するライブラリが格納されているJARファイルの名前です。複数のライブラリを指定するには、それらの名前をスペースで区切って指定します。

           注記
           ┌───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
           │注                                                                                                                                                                                                                                                                                                         │
           │                                                                                                                                                                                                                                                                                                           │
           │           ライブラリ・アーカイブ・ファイルはDASから削除されます。共通ライブラリおよび拡張ライブラリの場合は、サーバーの実行時にライブラリの削除が選択されるように、DASを再起動する必要があります。ライブラリを他のサーバー・インスタンスから削除するには、そのインスタンスを再起動してDASと同期させます。 │
           │                                                                                                                                                                                                                                                                                                           │
           │           このコマンドは、Windowsオペレーティング・システムではサポートされていません。                                                                                                                                                                                                                   │
           └───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --type
           ライブラリ・タイプおよびライブラリを削除するGlassFish
           Serverディレクトリを指定します。有効な値は次のとおりです:

           common
               Commonクラス・ローダー・ディレクトリdomain-dir/libからライブラリ・ファイルを削除します。これはデフォルトです。

           ext
               Javaオプション・パッケージ・ディレクトリdomain-dir/lib/extからライブラリ・ファイルを削除します。

           app
               アプリケーション固有のクラス・ローダー・ディレクトリdomain-dir/lib/applibsからライブラリ・ファイルを削除します。

           これらのディレクトリの詳細は、Oracle GlassFish
           Serverア

オペランド
       library-name
           削除対象のライブラリが格納されているJARファイルの名前。

例
       例 1, ライブラリの削除
           この例では、デフォルトのサーバー・インスタンスにあるアプリケーション固有のクラス・ローダー・ディレクトリから、アーカイブ・ファイルmylib.jarのライブラリを削除します。

               asadmin> remove-library --type app mylib.jar
               Command remove-library executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       add-library(1), list-libraries(1)

       asadmin(1M)

       Oracle GlassFish Serverア

                                2011年11月16日               REMOVE-LIBRARY(1)
