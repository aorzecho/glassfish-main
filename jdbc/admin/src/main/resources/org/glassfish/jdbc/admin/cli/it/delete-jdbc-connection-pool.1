DELETE-JDBC-CONNEC(1)                                    DELETE-JDBC-CONNEC(1)

NOME
       delete-jdbc-connection-pool - rimuove il connection pool JDBC
       specificato

SINTASSI
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

DESCRIZIONE
       Il comando secondario delete-jdbc-connection-pool elimina un connection
       pool JDBC. Prima di eseguire tale comando secondario, è necessario
       rimuovere tutte le associazioni al connection pool JDBC.

       Questo comando secondario è supportato solo in modalità remota.

OPZIONI
       --help, -?
           Visualizza il testo della Guida per il comando secondario.

       --cascade
           Se l'opzione viene impostata su true, vengono eliminate tutte le
           risorse JDBC associate al pool, tranne quest'ultimo. Se l'opzione
           viene impostata su false, l'eliminazione del pool non riesce se al
           pool sono associate risorse. Le risorse devono essere eliminate
           esplicitamente oppure l'opzione deve essere impostata su true. Il
           valore predefinito è false.

       --target
           Non è necessario specificare questa opzione, che è stata mantenuta
           solo per motivi di compatibilità con release precedenti. Se
           l'opzione viene specificata non si verifica un errore di sintassi.
           Il comando secondario viene eseguito correttamente e viene
           visualizzato un messaggio di avvertenza che indica che l'opzione
           viene ignorata.

OPERANDI
       jdbc_connection_pool_id
           Nome della risorsa JDBC da rimuovere.

ESEMPI
       Esempio 1, Eliminazione di un connection pool JDBC
           In questo esempio viene eliminato il connection pool JDBC
           denominato sample_derby_pool.

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

STATO DI USCITA
       0
           esecuzione del comando secondario riuscita

       1
           errore durante l'esecuzione del comando secondario

VEDERE ANCHE
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

                               17 settembre 2010         DELETE-JDBC-CONNEC(1)
