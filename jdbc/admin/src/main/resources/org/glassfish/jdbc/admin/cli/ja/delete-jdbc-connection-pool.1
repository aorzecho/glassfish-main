DELETE-JDBC-CONNEC(1)                                    DELETE-JDBC-CONNEC(1)

名前
       delete-jdbc-connection-pool - 指定したJDBC接続プールを削除します

概要
           delete-jdbc-connection-pool [--help]
           [--cascade={false|true}]
           [--target target]
           jdbc_connection_pool_id

説明
       delete-jdbc-connection-poolサブコマンドは、JDBC接続プールを削除します。このサブコマンドを実行する前に、JDBC接続プールのすべてのアソシエーションを削除する必要があります。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

       --cascade
           オプションがtrueに設定されている場合は、プール自体は残りますが、プールに関連付けられているすべてのJDBCリソースが削除されます。falseに設定したときは、リソースがプールに関連付けられている場合は、プールの削除に失敗します。リソースを明示的に削除するか、またはこのオプションをtrueに設定する必要があります。デフォルト値はfalseです。

       --target
           このオプションは指定しないでください。このオプションは以前のリリースとの互換性を維持するために使用されます。このオプションを指定すると、構文エラーが発生しません。かわりに、サブコマンドは正常に実行され、オプションが無視されたという警告メッセージが表示されます。

オペランド
       jdbc_connection_pool_id
           削除されるJDBCリソースの名前です。

例
       例 1, JDBC接続プールの削除
           この例では、sample_derby_pool JDBC接続プールを削除します。

               asadmin> delete-jdbc-connection-pool --cascade=false sample_derby_pool
               Command delete-jdbc-connection-pool executed correctly.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jdbc-connection-pool(1),list-jdbc-connection-pools(1)

       asadmin(1M)

                                 2010年9月17日           DELETE-JDBC-CONNEC(1)
