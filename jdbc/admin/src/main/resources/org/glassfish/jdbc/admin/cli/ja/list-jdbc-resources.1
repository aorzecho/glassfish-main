LIST-JDBC-RESOURCE(1)                                    LIST-JDBC-RESOURCE(1)

名前
       list-jdbc-resources - すべてのJDBCリソースを一覧表示します

概要
           list-jdbc-resources [--help] [target target]

説明
       list-jdbc-resourcesサブコマンドは、既存のJDBCリソースのリストを表示します。

       このサブコマンドは、リモート・モードでのみサポートされています。

オプション
       --help, -?
           サブコマンドに関するヘルプ・テキストが表示されます。

オペランド
       --target
           このオペランドは、一覧表示するJDBCリソースを指定します。このオペランドの使用はオプションです。有効な値は次のとおりです:

           server
               現在のサーバー内のJDBCリソースを一覧表示します(デフォルト)。

           domain
               現在のドメイン内のJDBCリソースを一覧表示します。

           cluster_name
               クラスタ内のJDBCリソースを一覧表示します。

           instance_name
               特定のインスタンス内のJDBCリソースを一覧表示します。

例
       例 1, JDBCリソースの一覧表示
           この例では、現在のJDBCリソースを一覧表示します。

               asadmin> list-jdbc-resources
               jdbc/DerbyPool
               Command list-jdbc-resources executed successfully.

終了ステータス
       0
           サブコマンドは正常に実行されました

       1
           サブコマンドの実行時にエラーが発生しました

関連項目
       create-jdbc-resource(1), delete-jdbc-resource(1)

       asadmin(1M)

                                 2010年9月17日           LIST-JDBC-RESOURCE(1)
