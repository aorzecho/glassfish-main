DELETE-JDBC-RESOUR(1)                                    DELETE-JDBC-RESOUR(1)

NOME
       delete-jdbc-resource - remove um recurso JDBC com o nome JNDI
       especificado

SINOPSE
           delete-jdbc-resource [--help] [--target target] jndi_name

DESCRIçãO
       O subcomando delete-jdbc-resource remove um recurso JDBC. Assegure-se
       de que todas as associações ao recurso JDBC sejam removidas antes de
       executar o subcomando.

       Esse subcomando só é suportado no modo remoto.

OPçõES
       --help, -?
           Exibe o texto de ajuda do subcomando.

       --target
           Essa opção ajuda a especificar o destino do qual o recurso JDBC
           está sendo removido. Os destinos válidos são:

           servidor
               Remove o recurso a partir da instância de servidor default.
               Este é o valor default.

           domain
               Remove o recurso a partir do domínio.

           cluster_name
               Remove o recurso a partir de cada instância do servidor do
               cluster.

           instance_name
               Remove o recurso a partir de uma instância de servidor
               particular.

           Nota
           ┌────────────────────────────────────────┐
           │Observação                              │
           │                                        │
           │           Os recursos sempre são       │
           │           criados para um domínio como │
           │           um todo, mas só estarão      │
           │           ativos para destinos para os │
           │           quais <resource-ref> tiver   │
           │           sido criado usando a opção   │
           │           --target durante a criação   │
           │           do recurso. Isso significa   │
           │           que a exclusão de um recurso │
           │           só deleta o elemento         │
           │            <resource-ref> para o       │
           │           --target especificado e não  │
           │           deleta o recurso do domínio  │
           │           como um todo, a não ser que  │
           │           o domain seja especificado   │
           │           como o --domain para a       │
           │           deleção.                     │
           └────────────────────────────────────────┘

OPERANDOS
       jndi_name
           O nome JNDI desse recurso JDBC a ser removido.

EXEMPLOS
       Exemplo 1, Deletando um recurso JDBC
           O exemplo a seguir deleta o recurso JDBC denominado jdbc/DerbyPool.

               asadmin> delete-jdbc-resource jdbc/DerbyPool
               Command delete-jdbc-resource executed successfully.

STATUS DE SAíDA
       0
           subcomando executado com sucesso

       1
           erro ao executar o subcomando

CONSULTE TAMBéM
       create-jdbc-resource(1), list-jdbc-resources(1)

       asadmin(1M)

                            11 de fevereiro de 2011      DELETE-JDBC-RESOUR(1)
